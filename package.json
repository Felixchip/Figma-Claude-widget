{
  "name": "figma-claude-widget",
  "version": "0.1.0",
  "description": "A Figma/FigJam widget that chats with Claude.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.71.0",
    "@figma/widget-typings": "^1.13.0",
    "typescript": "^5.3.0"
  }
}
